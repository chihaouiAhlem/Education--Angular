import { FiltreUniquePipe } from './filtre-unique.pipe';

describe('FiltreUniquePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreUniquePipe();
    expect(pipe).toBeTruthy();
  });
});
