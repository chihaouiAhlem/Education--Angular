import { CustumGenderPipe } from './custum-gender.pipe';

describe('CustumGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new CustumGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
