import { CustumPricePipe } from './custum-price.pipe';

describe('CustumPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CustumPricePipe();
    expect(pipe).toBeTruthy();
  });
});
