import { CustumFilterPipe } from './custum-filter.pipe';

describe('CustumFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustumFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
