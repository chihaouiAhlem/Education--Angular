import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'filtreUnique'
})
export class FiltreUniquePipe implements PipeTransform {

  transform() {
    
    ////pas encore fait
    
  }

}
